/**
 * declare Action types
 * If we export each types when we call these types by
 * import * as types from '(~)./ActionsTypes'
 */

// Example: 앞에는 리듀서 이름을 적어준다
export const INCREMENT = 'home/INCREMENT';